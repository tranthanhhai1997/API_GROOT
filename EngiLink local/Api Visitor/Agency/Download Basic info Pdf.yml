info:
  name: Download Basic info Pdf
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/applicants/download-basic-info-pdf?id=1"
  headers:
    - name: Jil-Public-Key
      value: "{{Jil-Public-Key}}"
    - name: Jil_Token
      value: "{{Jil_Token}}"
  params:
    - name: id
      value: "1"
      type: query
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
