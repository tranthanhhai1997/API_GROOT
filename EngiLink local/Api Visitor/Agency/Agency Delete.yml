info:
  name: Agency Delete
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/agency/delete"
  headers:
    - name: Jil-Public-Key
      value: "{{Jil-Public-Key}}"
    - name: Jil_Token
      value: "{{Jil_Token}}"
  params:
    - name: id
      value: "5"
      type: query
      disabled: true
  body:
    type: multipart-form
    data:
      - name: id
        type: text
        value: "5"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
