info:
  name: Get Agency Detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/agency/detail?id=3"
  headers:
    - name: Jil-Public-Key
      value: "{{Jil-Public-Key}}"
    - name: Jil_Token
      value: "{{Jil_Token}}"
  params:
    - name: id
      value: "3"
      type: query
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
