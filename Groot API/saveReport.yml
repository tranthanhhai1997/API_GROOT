info:
  name: saveReport
  type: http
  seq: 1

http:
  method: POST
  url: https://www.api.groot.local/user/save-report
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
